<dialog class="article-dialog" aria-labelledby="article-viewer-title">
  <div class="article-dialog-content"></div>
</dialog>
<p class="sr-only" role="status" data-article-status></p>
<script>
  import '../scripts/article-viewer';
</script>
<style is:global>
  .article-dialog {
    position: fixed;
    inset: 0;
    margin: 0;
    width: 100%;
    height: 100dvh;
    max-width: none;
    max-height: none;
    padding: clamp(20px, 4vw, 48px);
    border: 0;
    background: transparent;
    color: white;
    overflow-y: auto;
    overscroll-behavior: contain;
  }
  .article-dialog::backdrop { background: rgba(5, 12, 20, .65); backdrop-filter: blur(10px); }
  .article-dialog-content { width: 100%; max-width: 42rem; margin: 0 auto; }
  .article-dialog .article-pane { background: rgba(14, 25, 35, .94); }
  .article-dialog h1:focus { outline: none; }
  .article-flight { position: fixed; transform-style: preserve-3d; pointer-events: none; z-index: 1; }
  .article-flight-face { position: absolute; inset: 0; backface-visibility: hidden; border-radius: 1.5rem; overflow: hidden; }
  .article-flight-front { background: #13212c; }
  .article-flight-back { transform: rotateY(180deg); background: #0e1923; }
  .article-flight-front > * { width: 100%; height: 100%; transform: none !important; transition: none !important; }
  .article-flight-back .article-pane { min-height: 100%; }
  a[data-article-link][aria-busy="true"] { cursor: progress; }
</style>
